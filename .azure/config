#
# Jonathan Cormier's az configuration
#
# https://learn.microsoft.com/en-us/cli/azure/azure-cli-configuration
#
#
[cloud]
name = AzureCloud

[core]
first_run = yes

# Allowed values: json (default), jsonc, yaml, yamlc, table, tsv, and none.
output = yamlc

disable_confirm_prompt = yes
collect_telemetry = no

[logging]
enable_log_file = yes

[extension]
# Allowed values: no (default), yes_prompt, yes_without_prompt
use_dynamic_install = no
